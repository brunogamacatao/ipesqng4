import { EstaAutenticadoDirective } from './esta-autenticado.directive';

describe('EstaAutenticadoDirective', () => {
  it('should create an instance', () => {
    const directive = new EstaAutenticadoDirective();
    expect(directive).toBeTruthy();
  });
});
